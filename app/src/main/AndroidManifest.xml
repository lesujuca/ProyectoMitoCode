<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lerma.projectfinal">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".AndroidApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.ProjectFinal">
        <activity
            android:name=".activities.SplashActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.MenuActivity"
            android:label="@string/str_blank"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.ProductActivity"
            android:label="@string/str_product"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.ProductDetail"
            android:label="@string/str_product_detail"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.OrderActivity"
            android:label="@string/str_order"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.OrderDetail"
            android:label="@string/str_order_detail"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.RegisterActivity"
            android:label="@string/str_register_now"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.MainActivity"
            android:screenOrientation="portrait">
        </activity>
    </application>

</manifest>